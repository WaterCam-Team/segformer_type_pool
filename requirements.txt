# Versions this code is known to run on (conda env: python 3.9).
# Install torch first, matched to your CUDA, then the rest.

torch==1.13.1+cu116          # --extra-index-url https://download.pytorch.org/whl/cu116
torchvision==0.14.1+cu116
mmcv-full==1.7.2             # must be built against the torch/CUDA above

# open_clip is NO LONGER REQUIRED. The CLIP text tower is built from the
# vendored mmseg/models/clip and its weights come from the checkpoint, so
# nothing is downloaded. Only the unused Difficulty_PromptLearner still
# imports it, lazily.
ftfy==6.3.1                  # vendored CLIP tokenizer dependency
regex==2024.11.6             # vendored CLIP tokenizer dependency
timm==1.0.22

numpy==1.26.3
opencv-python==4.5.1.48
Pillow==11.0.0
terminaltables==3.1.10       # result table in tools/eval.py
matplotlib
